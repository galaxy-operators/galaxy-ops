- name: gflow
  version: v{{VERSION}}
  origin_addr:
    url: "{{ART_REPO}}/v{{VERSION}}/galaxy-ops-v{{VERSION}}-{{RUN_TARGET}}.tar.gz"
  cache_enable: false  
  cache_addr :
    url: "{{ART_CACHE_REPO}}"
  local: orion-ops.tar.gz
