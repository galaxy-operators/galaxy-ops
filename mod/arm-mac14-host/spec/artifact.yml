- name: gops
  version: ${USED_VER}
  origin_addr:
    url: "{{ART_REPO}}/${USED_VER}/galaxy-ops-${USED_VER}-aarch64-apple-darwin.tar.gz"
  cache_enable: false  
  cache_addr :
    url: "{{ART_CACHE_REPO}}"
  local: galaxy-ops-${USED_VER}.tar.gz
