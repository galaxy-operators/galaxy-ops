- name: gops
  version: ${USED_VER}
  origin_addr:
    url: "{{ART_REPO}}/${USED_VER}/galaxy-ops-${USED_VER}-x86_64-unknown-linux-gnu.tar.gz"
  cache_enable: false  
  cache_addr :
    url: "{{ART_CACHE_REPO}}"
  local: galaxy-ops-${USED_VER}.tar.gz
